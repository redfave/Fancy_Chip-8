<Window x:Class="Fancy_Chip_8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:Fancy_Chip_8.Core"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow"
        Width="525"
        Height="350"
        mc:Ignorable="d">
    <Window.DataContext>
        <core:Manager />
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="{Binding Path=CommandClose}" />
    </Window.InputBindings>
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Command="{Binding Path=CommandOpen}" Header="Open" />
                    <MenuItem Command="{Binding Path=CommandClose}" Header="Exit" />
                </MenuItem>
                <MenuItem Header="About" />
            </Menu>
            <Grid DockPanel.Dock="Bottom">
                <StackPanel>
                    <CheckBox Content="Run" IsChecked="{Binding Path=SystemIsRunning}"
                              IsEnabled="{Binding Path=ProgramIsLoaded}"/>
                </StackPanel>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
